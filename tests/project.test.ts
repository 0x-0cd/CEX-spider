/**
 * Simple test to verify the project structure
 */
import { describe, it, expect } from '@jest/globals';

describe('CEX Spider Project', () => {
  it('should have a working test environment', () => {
    expect(true).toBe(true);
  });
});